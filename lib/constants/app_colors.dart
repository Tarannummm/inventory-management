import 'package:flutter/material.dart';

class AppColors {
  // 🌊 PRIMARY BLUES
  static const Color primary = Color(0xff06234b); // deep navy
  static const Color secondary = Color(0xff324e79); // rich blue
  static const Color accent = Color(0xFF7DA0CA); // soft blue

  // 🎨 UI COLORS
  static const Color background = Color(0xFFF4F8FB); // very light blue
  static const Color card = Colors.white;

  // ⚠ STATUS COLORS
  static const Color danger = Color(0xFFD32F2F);
  static const Color success = Color(0xFF2E7D32);
}
